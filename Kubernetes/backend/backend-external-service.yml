apiVersion: v1
kind: Service
metadata:
  namespace: microservices
  name: internal-service
spec:
  selector:
    app: translation-microservice
  type: LoadBalancer
  ports:
     - port: 3502
       targetPort: 80
